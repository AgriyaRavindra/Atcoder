#include <bits/stdc++.h>
using namespace std;
     
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int a;
    cin>>a;
    int ans = a + a*a + a*a*a;
    cout<<ans<<"\n";
     
    return 0;
}